name = "purple-band-0824"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Cron trigger - runs every 15 minutes
[triggers]
crons = ["*/15 * * * *"]

# Environment variables (secrets are set in Cloudflare Dashboard)
# ANTHROPIC_API_KEY - set via `wrangler secret put ANTHROPIC_API_KEY`

[vars]
FIREBASE_URL = "https://monkey-social-250aa-default-rtdb.firebaseio.com"
